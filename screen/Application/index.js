import React from 'react';
import { View, Text } from 'react-native';

export default function ApplicationScreen({navigation}) {
    return (
        <View>
            <Text>Connect or Apply for agency</Text>
        </View>
    )
}